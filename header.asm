header:
	; Cetak pesan
	mov eax, msg1
	mov ebx, 1 ;stdout
	call printStr
	
	; Cetak pesan
	mov eax, msg2
	mov ebx, 1 ;stdout
	call printStr
	
	; Cetak pesan
	mov eax, msg3
	mov ebx, 1 ;stdout
	call printStr
	
	; Cetak pesan
	mov eax, msg4
	mov ebx, 1 ;stdout
	call printStr
	
	; Cetak pesan
	mov eax, msg5
	mov ebx, 1 ;stdout
	call printStr
	
ret